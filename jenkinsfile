node(){

         // deleteDir() // To wipe out Workspace
          stage 'Build Initialize'
			sh "mvn -version"
			sh "rm -rf *"
			sh 'git clone https://github.com/bannumails/gitdemo.git/'
			sh "ls -a"
      }
